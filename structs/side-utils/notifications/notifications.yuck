(defwidget notifications [text onclick ?class ?halign ?hexpand]
    (button :onclick onclick
            :active true
            :class "notif-btn"
            :halign halign
            :hexpand hexpand
      (label :text text
             :class "notif-label ${class}")))


; vim:ft=yuck:nowrap
