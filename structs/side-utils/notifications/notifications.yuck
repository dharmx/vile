(defwidget notifications [text onclick ?class]
    (button :onclick onclick
            :active true
            :class "notif-btn"
      (label :text text
             :class "notif-label ${class}")))


; vim:ft=yuck:nowrap
